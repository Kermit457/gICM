# OPUS 67: VS Code Extension
## Marketplace Distribution

---

## Overview

**Goal:** One-click install from VS Code Marketplace

```
User opens VS Code → Extensions → Search "OPUS 67" → Install → Done
```

**Time to build:** 3-4 weeks
**Cost:** $0 (Free publisher account)
**Distribution:** VS Code Marketplace (30M+ users)

---

## User Experience

### Installation

```
┌─────────────────────────────────────────────────────────────────────┐
│  VS Code Marketplace                                                │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  🚀 OPUS 67 - The Killer AI Engine                                 │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━                                 │
│                                                                     │
│  ⭐⭐⭐⭐⭐ (4.9) | 50K+ installs | ICM Motion                       │
│                                                                     │
│  [Install]  [Disable]  [Uninstall]                                  │
│                                                                     │
│  ─────────────────────────────────────────────────────────────────  │
│                                                                     │
│  Claude + Superpowers = OPUS 67                                     │
│                                                                     │
│  130 pre-built skills | 47 connected tools | 82 specialist agents  │
│                                                                     │
│  Features:                                                          │
│  • Instant AI superpowers - no configuration needed                │
│  • Solana-native development tools                                  │
│  • Works with Claude, Continue, Codeium, Copilot                   │
│  • Skill browser sidebar                                            │
│  • Agent switcher                                                   │
│  • Mode selector                                                    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### After Installation

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│  VS Code with OPUS 67                                                           │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  ┌────────────────┐  ┌──────────────────────────────────────────────────────┐  │
│  │ EXPLORER       │  │  Editor                                              │  │
│  │                │  │                                                       │  │
│  ├────────────────┤  │  // src/bonding-curve.ts                             │  │
│  │ 🚀 OPUS 67    │  │                                                       │  │
│  │                │  │  import { Jupiter } from '@jup-ag/core';             │  │
│  │ ▼ SKILLS      │  │                                                       │  │
│  │   ▶ Solana    │  │                                                       │  │
│  │   ▶ Frontend  │  │                                                       │  │
│  │   ▶ AI/LLM    │  │                                                       │  │
│  │   ▶ DevOps    │  │                                                       │  │
│  │                │  │                                                       │  │
│  │ ▼ AGENTS      │  │                                                       │  │
│  │   ● DeFi Arch │  │                                                       │  │
│  │   ○ Anchor Ex │  │                                                       │  │
│  │   ○ Frontend  │  │                                                       │  │
│  │                │  │                                                       │  │
│  │ ▼ MODES       │  │                                                       │  │
│  │   ● Vibe      │  │                                                       │  │
│  │   ○ Architect │  │                                                       │  │
│  │   ○ Audit     │  │                                                       │  │
│  │                │  │                                                       │  │
│  └────────────────┘  └──────────────────────────────────────────────────────┘  │
│                                                                                  │
│  ┌──────────────────────────────────────────────────────────────────────────┐  │
│  │ $(rocket) OPUS 67: DeFi Architect | Vibe Mode | 130 Skills Active       │  │
│  └──────────────────────────────────────────────────────────────────────────┘  │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## Feature Set

### 1. Sidebar Panel

```
┌─────────────────────────────────────────────────────────────────────┐
│  OPUS 67 SIDEBAR                                                    │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  🔍 Search skills, agents...                                        │
│                                                                     │
│  ━━━ SKILLS (130) ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                                     │
│  ▼ Solana (15)                                                      │
│    📘 solana-anchor-expert                                          │
│       Anchor framework mastery                                      │
│       [Load] [View] [Copy]                                          │
│                                                                     │
│    📘 jupiter-trader                                                │
│       Token swaps via Jupiter                                       │
│       [Load] [View] [Copy]                                          │
│                                                                     │
│    📘 pump-fun-expert                                               │
│       Pump.fun token launches                                       │
│       [Load] [View] [Copy]                                          │
│                                                                     │
│  ▶ AI/LLM (8)                                                       │
│  ▶ Frontend (12)                                                    │
│  ▶ Full-Stack (14)                                                  │
│  ▶ DevOps (12)                                                      │
│  ▶ GRAB (16)                                                        │
│                                                                     │
│  ━━━ QUICK ACTIONS ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                                     │
│  [🚀 New Solana Project]                                            │
│  [⚡ Quick Swap Setup]                                              │
│  [🎨 New Next.js App]                                               │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 2. Command Palette

```
> OPUS 67: Switch Agent
> OPUS 67: Load Skill
> OPUS 67: Set Mode
> OPUS 67: Browse Skills
> OPUS 67: Show Status
> OPUS 67: Open Dashboard
> OPUS 67: Configure MCPs
> OPUS 67: New Solana Project
> OPUS 67: Quick Swap Setup
> OPUS 67: Update Skills
```

### 3. Status Bar

```
┌─────────────────────────────────────────────────────────────────────┐
│ $(rocket) OPUS 67 | DeFi Architect | Vibe Mode | 130 Skills        │
└─────────────────────────────────────────────────────────────────────┘

Click segments to:
• OPUS 67 → Open dashboard
• Agent name → Switch agent
• Mode name → Switch mode
• Skills count → Browse skills
```

### 4. Context Menu Integration

```
Right-click in editor:
┌─────────────────────────────────────────────────────────────────────┐
│  Cut                                                                │
│  Copy                                                               │
│  Paste                                                              │
│  ─────────────────────────────────────────────────────────────────  │
│  🚀 OPUS 67                                                        │
│     ├── Explain this code                                           │
│     ├── Optimize with skill...                                      │
│     ├── Generate tests                                              │
│     ├── Add error handling                                          │
│     └── Convert to TypeScript                                       │
└─────────────────────────────────────────────────────────────────────┘
```

### 5. Code Actions (Lightbulb)

```typescript
// When cursor is on Solana code:
import { Connection } from '@solana/web3.js';
//      ^^^^^^^^^^
//      💡 OPUS 67: Use Helius for better RPC
//      💡 OPUS 67: Add connection retry logic
//      💡 OPUS 67: Load solana-reader skill
```

### 6. Snippets

```typescript
// Type "opus-swap" + Tab:
const swapResult = await jupiter.exchange({
  inputMint: ${1:inputMint},
  outputMint: ${2:outputMint},
  amount: ${3:amount},
  slippageBps: ${4:100},
});

// Type "opus-anchor" + Tab:
#[program]
pub mod ${1:program_name} {
    use super::*;
    
    pub fn ${2:initialize}(ctx: Context<${3:Initialize}>) -> Result<()> {
        Ok(())
    }
}
```

---

## Extension Architecture

### Project Structure

```
opus67-vscode/
├── package.json                 # Extension manifest
├── tsconfig.json
├── webpack.config.js
├── src/
│   ├── extension.ts            # Entry point
│   ├── commands/
│   │   ├── switchAgent.ts
│   │   ├── loadSkill.ts
│   │   ├── setMode.ts
│   │   └── index.ts
│   ├── providers/
│   │   ├── skillTreeProvider.ts
│   │   ├── agentTreeProvider.ts
│   │   ├── statusBarProvider.ts
│   │   └── codeActionProvider.ts
│   ├── services/
│   │   ├── configService.ts
│   │   ├── mcpService.ts
│   │   ├── aiIntegration.ts
│   │   └── skillLoader.ts
│   ├── views/
│   │   ├── sidebar.ts
│   │   ├── dashboard.ts
│   │   └── skillViewer.ts
│   └── utils/
│       ├── constants.ts
│       └── helpers.ts
├── resources/
│   ├── icons/
│   │   ├── opus67.svg
│   │   ├── skill.svg
│   │   ├── agent.svg
│   │   └── mode.svg
│   ├── skills/
│   │   └── [skills data]
│   ├── agents/
│   │   └── [agents data]
│   └── modes/
│       └── [modes data]
├── media/
│   ├── dashboard.html
│   ├── dashboard.css
│   └── dashboard.js
└── README.md
```

### package.json (Extension Manifest)

```json
{
  "name": "opus67",
  "displayName": "OPUS 67 - The Killer AI Engine",
  "description": "130 skills, 47 tools, 82 agents - Claude with superpowers",
  "version": "4.0.0",
  "publisher": "icm-motion",
  "icon": "resources/icons/opus67.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/icm-motion/opus67-vscode"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Programming Languages",
    "Snippets",
    "Other"
  ],
  "keywords": [
    "ai",
    "claude",
    "solana",
    "blockchain",
    "copilot",
    "assistant"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "opus67.switchAgent",
        "title": "OPUS 67: Switch Agent",
        "icon": "$(person)"
      },
      {
        "command": "opus67.loadSkill",
        "title": "OPUS 67: Load Skill",
        "icon": "$(book)"
      },
      {
        "command": "opus67.setMode",
        "title": "OPUS 67: Set Mode",
        "icon": "$(settings-gear)"
      },
      {
        "command": "opus67.browseSkills",
        "title": "OPUS 67: Browse Skills",
        "icon": "$(library)"
      },
      {
        "command": "opus67.showStatus",
        "title": "OPUS 67: Show Status"
      },
      {
        "command": "opus67.openDashboard",
        "title": "OPUS 67: Open Dashboard",
        "icon": "$(dashboard)"
      },
      {
        "command": "opus67.configureMCPs",
        "title": "OPUS 67: Configure MCPs"
      },
      {
        "command": "opus67.newSolanaProject",
        "title": "OPUS 67: New Solana Project",
        "icon": "$(rocket)"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "opus67",
          "title": "OPUS 67",
          "icon": "resources/icons/opus67.svg"
        }
      ]
    },
    "views": {
      "opus67": [
        {
          "id": "opus67.skills",
          "name": "Skills",
          "icon": "$(book)"
        },
        {
          "id": "opus67.agents",
          "name": "Agents",
          "icon": "$(person)"
        },
        {
          "id": "opus67.modes",
          "name": "Modes",
          "icon": "$(settings-gear)"
        },
        {
          "id": "opus67.quickActions",
          "name": "Quick Actions",
          "icon": "$(zap)"
        }
      ]
    },
    "menus": {
      "editor/context": [
        {
          "submenu": "opus67.context",
          "group": "opus67"
        }
      ],
      "opus67.context": [
        {
          "command": "opus67.explainCode",
          "group": "1_explain"
        },
        {
          "command": "opus67.optimizeWithSkill",
          "group": "2_optimize"
        },
        {
          "command": "opus67.generateTests",
          "group": "3_generate"
        }
      ],
      "view/title": [
        {
          "command": "opus67.refreshSkills",
          "when": "view == opus67.skills",
          "group": "navigation"
        }
      ]
    },
    "submenus": [
      {
        "id": "opus67.context",
        "label": "🚀 OPUS 67"
      }
    ],
    "configuration": {
      "title": "OPUS 67",
      "properties": {
        "opus67.defaultAgent": {
          "type": "string",
          "default": "full-stack",
          "description": "Default agent to use"
        },
        "opus67.defaultMode": {
          "type": "string",
          "default": "vibe",
          "description": "Default mode"
        },
        "opus67.autoLoadSkills": {
          "type": "boolean",
          "default": true,
          "description": "Automatically load relevant skills"
        },
        "opus67.showStatusBar": {
          "type": "boolean",
          "default": true,
          "description": "Show OPUS 67 in status bar"
        },
        "opus67.aiProvider": {
          "type": "string",
          "enum": ["claude", "continue", "codeium", "copilot", "manual"],
          "default": "claude",
          "description": "AI provider integration"
        }
      }
    },
    "snippets": [
      {
        "language": "typescript",
        "path": "./snippets/typescript.json"
      },
      {
        "language": "rust",
        "path": "./snippets/rust.json"
      }
    ],
    "keybindings": [
      {
        "command": "opus67.loadSkill",
        "key": "ctrl+shift+o",
        "mac": "cmd+shift+o"
      },
      {
        "command": "opus67.switchAgent",
        "key": "ctrl+shift+a",
        "mac": "cmd+shift+a"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "webpack",
    "watch": "webpack --watch",
    "package": "webpack --mode production --devtool hidden-source-map",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4",
    "ts-loader": "^9.5.1"
  }
}
```

### src/extension.ts

```typescript
import * as vscode from 'vscode';
import { SkillTreeProvider } from './providers/skillTreeProvider';
import { AgentTreeProvider } from './providers/agentTreeProvider';
import { ModeTreeProvider } from './providers/modeTreeProvider';
import { StatusBarProvider } from './providers/statusBarProvider';
import { CodeActionProvider } from './providers/codeActionProvider';
import { ConfigService } from './services/configService';
import { registerCommands } from './commands';

let statusBarProvider: StatusBarProvider;

export async function activate(context: vscode.ExtensionContext) {
  console.log('OPUS 67 is activating...');

  // Initialize services
  const configService = new ConfigService(context);
  await configService.initialize();

  // Register tree view providers
  const skillTreeProvider = new SkillTreeProvider(configService);
  const agentTreeProvider = new AgentTreeProvider(configService);
  const modeTreeProvider = new ModeTreeProvider(configService);

  vscode.window.registerTreeDataProvider('opus67.skills', skillTreeProvider);
  vscode.window.registerTreeDataProvider('opus67.agents', agentTreeProvider);
  vscode.window.registerTreeDataProvider('opus67.modes', modeTreeProvider);

  // Register status bar
  statusBarProvider = new StatusBarProvider(configService);
  context.subscriptions.push(statusBarProvider);

  // Register code actions
  const codeActionProvider = new CodeActionProvider(configService);
  context.subscriptions.push(
    vscode.languages.registerCodeActionsProvider(
      ['typescript', 'javascript', 'rust'],
      codeActionProvider
    )
  );

  // Register commands
  registerCommands(context, configService, {
    skillTreeProvider,
    agentTreeProvider,
    modeTreeProvider,
    statusBarProvider,
  });

  // Show welcome message on first install
  const isFirstInstall = !context.globalState.get('opus67.installed');
  if (isFirstInstall) {
    await context.globalState.update('opus67.installed', true);
    showWelcomeMessage();
  }

  // Update status bar
  statusBarProvider.update();

  console.log('OPUS 67 activated successfully!');
}

export function deactivate() {
  if (statusBarProvider) {
    statusBarProvider.dispose();
  }
}

function showWelcomeMessage() {
  vscode.window
    .showInformationMessage(
      '🚀 OPUS 67 installed! 130 skills, 47 tools, 82 agents ready.',
      'Open Dashboard',
      'Browse Skills'
    )
    .then((selection) => {
      if (selection === 'Open Dashboard') {
        vscode.commands.executeCommand('opus67.openDashboard');
      } else if (selection === 'Browse Skills') {
        vscode.commands.executeCommand('opus67.browseSkills');
      }
    });
}
```

### src/providers/skillTreeProvider.ts

```typescript
import * as vscode from 'vscode';
import { ConfigService } from '../services/configService';

interface Skill {
  id: string;
  name: string;
  description: string;
  category: string;
  tokens: number;
}

export class SkillTreeProvider implements vscode.TreeDataProvider<SkillItem> {
  private _onDidChangeTreeData = new vscode.EventEmitter<SkillItem | undefined>();
  readonly onDidChangeTreeData = this._onDidChangeTreeData.event;

  constructor(private configService: ConfigService) {}

  refresh(): void {
    this._onDidChangeTreeData.fire(undefined);
  }

  getTreeItem(element: SkillItem): vscode.TreeItem {
    return element;
  }

  async getChildren(element?: SkillItem): Promise<SkillItem[]> {
    if (!element) {
      // Root level - show categories
      const categories = this.configService.getSkillCategories();
      return categories.map(
        (cat) =>
          new SkillItem(
            cat.name,
            `${cat.count} skills`,
            vscode.TreeItemCollapsibleState.Collapsed,
            'category',
            cat.id
          )
      );
    }

    if (element.type === 'category') {
      // Show skills in category
      const skills = this.configService.getSkillsByCategory(element.categoryId!);
      return skills.map(
        (skill) =>
          new SkillItem(
            skill.id,
            skill.description,
            vscode.TreeItemCollapsibleState.None,
            'skill',
            undefined,
            skill
          )
      );
    }

    return [];
  }
}

class SkillItem extends vscode.TreeItem {
  constructor(
    public readonly label: string,
    public readonly description: string,
    public readonly collapsibleState: vscode.TreeItemCollapsibleState,
    public readonly type: 'category' | 'skill',
    public readonly categoryId?: string,
    public readonly skill?: Skill
  ) {
    super(label, collapsibleState);

    this.tooltip = this.description;

    if (type === 'skill') {
      this.iconPath = new vscode.ThemeIcon('book');
      this.contextValue = 'skill';
      this.command = {
        command: 'opus67.viewSkill',
        title: 'View Skill',
        arguments: [this.skill],
      };
    } else {
      this.iconPath = new vscode.ThemeIcon('folder');
      this.contextValue = 'category';
    }
  }
}
```

### src/providers/statusBarProvider.ts

```typescript
import * as vscode from 'vscode';
import { ConfigService } from '../services/configService';

export class StatusBarProvider {
  private statusBarItem: vscode.StatusBarItem;
  private agentItem: vscode.StatusBarItem;
  private modeItem: vscode.StatusBarItem;

  constructor(private configService: ConfigService) {
    // Main status
    this.statusBarItem = vscode.window.createStatusBarItem(
      vscode.StatusBarAlignment.Left,
      100
    );
    this.statusBarItem.command = 'opus67.openDashboard';

    // Agent status
    this.agentItem = vscode.window.createStatusBarItem(
      vscode.StatusBarAlignment.Left,
      99
    );
    this.agentItem.command = 'opus67.switchAgent';

    // Mode status
    this.modeItem = vscode.window.createStatusBarItem(
      vscode.StatusBarAlignment.Left,
      98
    );
    this.modeItem.command = 'opus67.setMode';

    // Show all
    this.statusBarItem.show();
    this.agentItem.show();
    this.modeItem.show();
  }

  update() {
    const config = this.configService.getConfig();
    const skillCount = this.configService.getSkillCount();

    this.statusBarItem.text = `$(rocket) OPUS 67`;
    this.statusBarItem.tooltip = `OPUS 67 v4.0 - ${skillCount} skills active`;

    this.agentItem.text = `$(person) ${config.currentAgent}`;
    this.agentItem.tooltip = 'Click to switch agent';

    this.modeItem.text = `$(settings-gear) ${config.currentMode}`;
    this.modeItem.tooltip = 'Click to change mode';
  }

  dispose() {
    this.statusBarItem.dispose();
    this.agentItem.dispose();
    this.modeItem.dispose();
  }
}
```

### src/commands/index.ts

```typescript
import * as vscode from 'vscode';
import { ConfigService } from '../services/configService';

export function registerCommands(
  context: vscode.ExtensionContext,
  configService: ConfigService,
  providers: any
) {
  // Switch Agent
  context.subscriptions.push(
    vscode.commands.registerCommand('opus67.switchAgent', async () => {
      const agents = configService.getAgents();
      const items = agents.map((a) => ({
        label: a.name,
        description: a.role,
        id: a.id,
      }));

      const selected = await vscode.window.showQuickPick(items, {
        placeHolder: 'Select an agent',
      });

      if (selected) {
        await configService.setCurrentAgent(selected.id);
        providers.statusBarProvider.update();
        vscode.window.showInformationMessage(
          `Switched to ${selected.label} agent`
        );
      }
    })
  );

  // Load Skill
  context.subscriptions.push(
    vscode.commands.registerCommand('opus67.loadSkill', async () => {
      const skills = configService.getAllSkills();
      const items = skills.map((s) => ({
        label: s.id,
        description: s.description,
        detail: `${s.category} | ${s.tokens} tokens`,
        skill: s,
      }));

      const selected = await vscode.window.showQuickPick(items, {
        placeHolder: 'Select a skill to load',
        matchOnDescription: true,
        matchOnDetail: true,
      });

      if (selected) {
        await configService.loadSkill(selected.skill);
        vscode.window.showInformationMessage(
          `Loaded skill: ${selected.label}`
        );
      }
    })
  );

  // Set Mode
  context.subscriptions.push(
    vscode.commands.registerCommand('opus67.setMode', async () => {
      const modes = configService.getModes();
      const items = modes.map((m) => ({
        label: m.name,
        description: m.description,
        id: m.id,
      }));

      const selected = await vscode.window.showQuickPick(items, {
        placeHolder: 'Select a mode',
      });

      if (selected) {
        await configService.setCurrentMode(selected.id);
        providers.statusBarProvider.update();
        vscode.window.showInformationMessage(
          `Switched to ${selected.label} mode`
        );
      }
    })
  );

  // Open Dashboard
  context.subscriptions.push(
    vscode.commands.registerCommand('opus67.openDashboard', () => {
      const panel = vscode.window.createWebviewPanel(
        'opus67Dashboard',
        'OPUS 67 Dashboard',
        vscode.ViewColumn.One,
        { enableScripts: true }
      );

      panel.webview.html = getDashboardHtml(configService);
    })
  );

  // More commands...
}

function getDashboardHtml(configService: ConfigService): string {
  const config = configService.getConfig();
  
  return `
    <!DOCTYPE html>
    <html>
    <head>
      <style>
        body { font-family: var(--vscode-font-family); padding: 20px; }
        .stat { display: inline-block; margin: 10px; padding: 20px; 
                background: var(--vscode-editor-background); border-radius: 8px; }
        .stat-value { font-size: 32px; font-weight: bold; }
        .stat-label { font-size: 14px; opacity: 0.7; }
      </style>
    </head>
    <body>
      <h1>🚀 OPUS 67 Dashboard</h1>
      
      <div class="stats">
        <div class="stat">
          <div class="stat-value">${config.skills.length}</div>
          <div class="stat-label">Skills</div>
        </div>
        <div class="stat">
          <div class="stat-value">${config.mcps.length}</div>
          <div class="stat-label">MCPs</div>
        </div>
        <div class="stat">
          <div class="stat-value">${config.agents.length}</div>
          <div class="stat-label">Agents</div>
        </div>
        <div class="stat">
          <div class="stat-value">${config.modes.length}</div>
          <div class="stat-label">Modes</div>
        </div>
      </div>

      <h2>Current Configuration</h2>
      <p><strong>Agent:</strong> ${config.currentAgent}</p>
      <p><strong>Mode:</strong> ${config.currentMode}</p>
    </body>
    </html>
  `;
}
```

---

## AI Provider Integrations

### Continue Extension

```typescript
// src/services/continueIntegration.ts

export class ContinueIntegration {
  async injectContext(skill: Skill) {
    // Find Continue's config
    const continueConfig = vscode.workspace.getConfiguration('continue');
    
    // Add skill to system prompt
    const currentPrompt = continueConfig.get('systemMessage') || '';
    const newPrompt = `${currentPrompt}\n\n${skill.content}`;
    
    await continueConfig.update('systemMessage', newPrompt);
  }
}
```

### Codeium Integration

```typescript
// src/services/codeiumIntegration.ts

export class CodeiumIntegration {
  async injectContext(skill: Skill) {
    // Codeium uses .codeium/config.json
    const configPath = path.join(
      vscode.workspace.rootPath || '',
      '.codeium',
      'config.json'
    );
    
    // Add skill context
    const config = await fs.readJson(configPath).catch(() => ({}));
    config.context = config.context || [];
    config.context.push({
      type: 'skill',
      id: skill.id,
      content: skill.content,
    });
    
    await fs.writeJson(configPath, config, { spaces: 2 });
  }
}
```

---

## Publishing

### 1. Create Publisher Account

```bash
# Install vsce
npm install -g @vscode/vsce

# Create publisher (one-time)
vsce create-publisher icm-motion
```

### 2. Package Extension

```bash
vsce package
# Creates opus67-4.0.0.vsix
```

### 3. Publish

```bash
vsce publish
# Or with token
vsce publish -p <token>
```

### 4. Marketplace Listing

```markdown
# OPUS 67 - The Killer AI Engine

Claude + Superpowers = OPUS 67

## Features

- 🚀 **130 Pre-built Skills** - Workflows for every task
- 🔧 **47 Connected Tools** - MCPs that actually work
- 👤 **82 Specialist Agents** - Domain experts on demand
- 🎯 **30 Behavioral Modes** - Adapt to any situation
- ⚡ **Solana-Native** - Built for Web3 developers

## Quick Start

1. Install the extension
2. Press `Cmd+Shift+O` to load a skill
3. Start building!

## Screenshots

[Screenshot 1: Sidebar]
[Screenshot 2: Command Palette]
[Screenshot 3: Status Bar]
```

---

## Timeline

| Week | Tasks |
|------|-------|
| 1 | Extension scaffold, package.json, basic activation |
| 2 | Sidebar providers, skill/agent/mode trees |
| 3 | Commands, status bar, context menus |
| 4 | AI integrations, dashboard, testing, publish |

---

## Success Metrics

| Metric | Target |
|--------|--------|
| Installs | 10K in first month |
| Rating | 4.5+ stars |
| Weekly active users | 2K+ |
| GitHub stars | 1K+ |
